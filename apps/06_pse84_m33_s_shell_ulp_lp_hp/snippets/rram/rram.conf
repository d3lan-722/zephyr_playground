# Link from the RRAM secure alias chosen by the rram snippet overlay
# (CONFIG_FLASH_BASE_ADDRESS=0x32011400) instead of the board's
# `m33s_xip` SMIF code-partition. Mirrors the approach used by
# apps/17_pse84_ds_ram_exact/m33_s/prj.conf.
CONFIG_USE_DT_CODE_PARTITION=n
